$(() => {
  $(".cSidLeftDash ul.nav li.nav-item a").each(function(i, e){
    var pageUrl = window.location.href.split(/[?#]/)[0];
    if (this.href == pageUrl){
      var check = pageUrl.split("/");
      if(check.slice(-1)[0] != 'home'){
        $(this).parent().addClass("active"); // add active to li of the current link
      }
      if($(this).hasClass('sub-link')){
        $(this).parent().parent().parent().parent().addClass('active');
        $(this).parent().parent().parent().prev().click(); // click the item to make it drop
      }
    }
  });
  // APLICAR ESTILOS A LOS MENÚS COLAPSADOS ANTES DE QUE TENGAN LA CLASE ACTIVE
  /*
  var menusCollapsed = $("li.nav-item");
  $(document).on("click","li.nav-item",function(){
    let divsCollapsed = $(this).find("div.collapse");
    if($(this).find("div.collapse").hasClass('in')){
      console.log("asdasdasd");
    }
  });
  */
  // $('#myModal').modal({
  //   backdrop: 'true',
  //   keyboard: true,
  //   focus: 'true',
  //   show: 'false'
  // });
  // var myModal = new bootstrap.Modal(document.getElementById('myModal'), {
  //   keyboard: false
  // });
});